package main

/*
import "fmt"

func main() {

	var city string // will default to ""

	switch city {
	case "Des Moines":
		fmt.Println("You live in Iowa")
	case "Minneapolis,", "St Paul":
		fmt.Println("You live in Minnesota")
	case "Madison":
		fmt.Println("You live in Wisconsin")
	default:
		fmt.Println("You're not from around here.")
	}

	// ****************************
	var i int // will default to 0

	switch {
	case i > 10:
		fmt.Println("Greater than 10")
	case i < 10:
		fmt.Println("Less than 10")
	default:
		fmt.Println("Is 10")
	}

	// ****************************
	var x int = 9

	switch {
	case x != 10:
		fmt.Println("Does not equal 10")
		fallthrough // will not continue to move through cases after finds a match unless fallthough is specified
	case x < 10:
		fmt.Println("Less than 10")
	case x > 10:
		fmt.Println("Greater than 10")
	default:
		fmt.Println("Is 10")
	}
}
*/
